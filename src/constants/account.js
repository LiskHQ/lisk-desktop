export const balanceNeededForInitialization = 2e7;
export const balanceNeededForReclaim = 1e6;
const account = {
  lockDuration: 10000, // lock duration time is 10 minutes in milliSecond
  warnLockDuration: 5000,
};

export default account;
