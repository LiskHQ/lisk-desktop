import delegates from '@tests/constants/delegates';
/* eslint-disable  max-lines, import/prefer-default-export */
export const mockDelegates = {
  meta: {
    count: 2,
    offset: 0,
    total: 30,
  },
  data: delegates,
};
