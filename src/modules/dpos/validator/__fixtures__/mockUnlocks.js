import { unlockList } from '@tests/constants/votes';
/* eslint-disable  max-lines, import/prefer-default-export */
export const mockUnlocks = {
  meta: {
    count: 2,
    offset: 0,
    total: 30,
  },
  data: unlockList,
};
