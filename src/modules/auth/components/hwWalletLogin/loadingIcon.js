import React from 'react';
import Icon from '@theme/Icon';
import styles from './loadingIcon.css';

const LoadingIcon = () => <Icon name="iconLoader" className={styles.loadingIcon} />;

export default LoadingIcon;
