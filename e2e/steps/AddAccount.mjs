import common from './common.mjs'/* eslint-disable new-cap */
import { Given, Then } from '@cucumber/cucumber';
import { expect } from '@playwright/test';

Then('I should be redirected to route: {string}', async function (route) {
  
});
